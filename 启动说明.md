# 启动 Cloudflare 清障环境

## 前置要求
- 安装 Docker Desktop
- 启动 Docker Desktop

## 启动步骤

### 1. 创建环境变量文件（可选）
```bash
# 如果不存在 .env 文件，创建一个
cat > .env << 'EOF'
# 端口配置
CHATGPT2API_PORT=3000
WARP_SOCKS_PORT=40000
PRIVOXY_PORT=40080
FLARESOLVERR_PORT=8191

# 时区
TZ=Asia/Shanghai

# FlareSolverr 日志级别
FLARESOLVERR_LOG_LEVEL=info

# Cloudflare WARP+ License Key（可选，提升速度）
# WARP_LICENSE_KEY=your-license-key-here
EOF
```

### 2. 启动完整服务栈
```bash
# 启动 WARP 代理环境
docker-compose -f docker-compose.warp.yml up -d

# 查看服务状态
docker-compose -f docker-compose.warp.yml ps

# 查看日志
docker-compose -f docker-compose.warp.yml logs -f app
```

### 3. 验证服务
```bash
# 检查 privoxy 是否可访问
curl -x http://localhost:40080 https://www.cloudflare.com/cdn-cgi/trace

# 检查 flaresolverr 是否运行
curl http://localhost:8191/

# 应该返回类似：{"status":"ready","message":"FlareSolverr is ready!"}
```

### 4. 停止服务
```bash
docker-compose -f docker-compose.warp.yml down
```

## 配置说明

配置文件 `config.json` 中的关键配置：

```json
{
  "proxy_runtime": {
    "enabled": true,  // 启用代理运行时
    "proxy_url": "http://privoxy:8118",  // Docker 内部访问
    "clearance": {
      "enabled": true,
      "mode": "flaresolverr",
      "flaresolverr_url": "http://flaresolverr:8191"
    }
  }
}
```

## 架构说明

```
注册请求 → chatgpt2api 容器
           ↓
         privoxy 代理 (端口 8118)
           ↓
         warp-proxy (Cloudflare WARP)
           ↓
         OpenAI API

遇到 Cloudflare 挑战时：
注册请求 → chatgpt2api 容器
           ↓
         flaresolverr (解决挑战)
           ↓
         获取 clearance cookie
           ↓
         重试请求（带 cookie）
```

## 故障排查

### 问题：Could not resolve proxy: privoxy
**原因**：不在 Docker 环境中运行，或 Docker 容器未启动

**解决**：
1. 确保 Docker Desktop 正在运行
2. 启动完整的服务栈：`docker-compose -f docker-compose.warp.yml up -d`
3. 或者禁用代理：在 `config.json` 中设置 `"proxy_runtime.enabled": false`

### 问题：privoxy 容器启动失败
**原因**：warp-proxy 未就绪

**解决**：
```bash
# 查看日志
docker logs chatgpt2api-warp-proxy

# 重启 WARP 容器
docker restart chatgpt2api-warp-proxy

# 等待 30 秒后重启 privoxy
docker restart chatgpt2api-privoxy
```

### 问题：注册仍然失败
**可能原因**：
1. WARP 未连接成功
2. FlareSolverr 无法解决挑战
3. IP 被封禁

**解决**：
```bash
# 检查 WARP 连接状态
docker exec chatgpt2api-warp-proxy curl -x socks5://127.0.0.1:1080 https://www.cloudflare.com/cdn-cgi/trace

# 应该看到 warp=on 或 warp=plus

# 重启整个服务栈
docker-compose -f docker-compose.warp.yml restart

# 查看详细日志
docker-compose -f docker-compose.warp.yml logs -f
```
